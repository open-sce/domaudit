apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "domaudit.fullname" . }}-test-connection"
  labels:
    {{- include "domaudit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "domaudit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
